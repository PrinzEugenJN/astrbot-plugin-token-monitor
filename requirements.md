# Token 水位监控插件 — 需求文档（v0.3 草案）

> 版本：v0.3 ｜ 日期：2026-08-03 ｜ 状态：待林审阅
> 数据源探路已完成（源码 + 数据库 + 接口三重验证），本草案基于事实，不含臆测。

---

## 一、背景与目标

**背景**：AstrBot 上下文有 100 万 token 上限，达到 82%（约 82 万）会触发自动压缩（源码硬编码）。压缩会丢弃大部分历史上下文，影响工作连续性。目前只能通过"问欧根"来查水位，且 dashboard 现有展示只覆盖单条消息/当天累计，无法一览全局。

**目标**：做一个 AstrBot 插件，在 dashboard 的**插件页面上**实时展示：
1. 各会话的上下文水位（占用 token、占上限百分比、距压缩阈值的距离）
2. 历史趋势与累计用量
3. 接近压缩阈值时**主动告警**（QQ 消息，可开关）

让林随时打开网页就能看到，不用再问。

---

## 二、范围

### 做（In Scope）
- ✅ 插件 Web 页面（dashboard 内嵌）：会话水位一览 + 趋势图 + 累计用量
- ✅ 压缩预警：后端定时检查，接近阈值时主动发 QQ 消息
- ✅ 告警**开关**做成设置项（随时可开关）
- ✅ 告警目标会话、检查间隔等参数做成设置项

### 暂缓 / 未来扩展（Deferred）
- ◽ QQ 内 `/ctx` 斜杠命令（在 QQ 里直接发命令查水位）：低优先级，本次不做，后续可扩展
- ◽ UI 风格美化：低优先级，**最小功能验证通过后再做**
- ◽ 告警历史记录、压缩事件标注、会话分组等增强（见第十一节）

### 不做（Out of Scope）
- ❌ 不修改 AstrBot 核心代码（红线，改源码会被版本更新覆盖）
- ❌ **不做任何数值的在线调整**：上下文上限（1M）、压缩阈值（82%）、告警阈值（75%）均为**固定值**，插件只展示。要改需手动改代码并沟通——保持原子化

---

## 三、功能需求

### 3.1 插件页面（dashboard 内嵌）

**入口**：插件管理 → 已安装插件 → 本插件 → 页面（AstrBot 原生机制，插件目录 `pages/` 自动发现）

**页面内容**：
1. **概览区**（顶部）：主会话（林 ↔ 欧根）水位大数字 + 进度条 + 距压缩阈值剩余空间
2. **会话水位列表**：
   - 列出**所有会话**（含未来可能的群聊），每项显示：标题/平台/用户、token_usage、占上限百分比、距压缩阈值、最后更新时间
   - **折叠机制**：按 token_usage 排序，用量少的默认折叠，可展开
   - **告警状态徽标**：当前水位超过告警阈值（75%）的会话，显示"⚠️ 告警中"标记并变色高亮
3. **趋势区**：累计用量趋势图（复用 `/api/stats/provider-tokens?days=1/3/7`，支持切换）
4. **实时性**：前端 10 秒轮询刷新 + **手动刷新按钮**（token 变化不快，轮询足够；不用 SSE）
5. **健壮性**：接口请求失败/无数据时显示友好提示（如"接口暂不可用，稍后自动重试"），不白屏

### 3.2 压缩告警（后端逻辑）

- 后端定时任务（建议 60 秒检查一次）读取各会话水位
- 达到告警阈值（**固定为上限的 75%**，即 750,000；比压缩阈值 82% 提前 7%，留足反应时间）时，向**指定 QQ 会话**发送提醒
- **去重（按会话独立）**：每个会话各自维护状态——水位从"低于阈值"到"高于阈值"只告警一次，直到该会话水位回落（如压缩后）才重新武装；多个会话同时超阈值则各自告警一次，不合并、不刷屏
- 告警消息内容示例：
  ```
  ⚠️ 上下文水位告警
  会话：欧根亲王（私聊）
  当前：760,000 / 1,000,000（76%）
  距压缩阈值（820,000）仅剩：60,000
  建议：尽快处理，避免触发自动压缩丢失上下文
  ```

---

## 四、数据源与口径（已探明）

| 数据 | 来源 | 说明 |
|---|---|---|
| 各会话水位 | `GET /api/conversations` → `token_usage` | 每次 LLM 请求后更新（INTEGER）；主会话当前约 9.2 万（压缩后基数） |
| 会话元信息 | 同上 | `platform_id / user_id / cid / title / persona_id / created_at / updated_at / umo_info` |
| 累计用量趋势 | `GET /api/stats/provider-tokens?days=1\|3\|7` | 按小时桶返回 `trend_by_provider`、`total_by_provider`、`total_by_umo`、`range_total_tokens`、`range_total_calls`、`today_total_tokens` 等 |
| 上限 / 压缩阈值 | **代码硬编码，无接口** | 上限 1,000,000；压缩阈值 82%（compressor.py `compression_threshold=0.82`）。**插件内固定常量，仅作展示参考** |

**口径注意**：
- `token_usage` 是"最近一次请求后的值"，不是实时计数器——页面需显示"最后更新于 XX:XX"
- 上限、压缩阈值、告警阈值为固定常量（1M / 82% / 75%），页面展示用；若将来核心改了值，需同步改插件代码

---

## 五、配置项（_conf_schema.json → 插件配置 UI）

> 仅保留"开关类/目标类/节奏类"参数；**数值类（上限、压缩阈值、告警阈值）固定，不进配置**。

| 配置键 | 类型 | 默认值 | 说明 |
|---|---|---|---|
| `alert_enabled` | bool | true | 告警总开关 |
| `alert_target_session` | string | （安装时填） | 告警发送目标会话，如 `aiocqhttp:private:256418297` |
| `check_interval_sec` | number | 60 | 后端水位检查间隔（秒） |
| `refresh_interval_ms` | number | 10000 | 页面轮询间隔（毫秒） |

**固定常量**（代码内，非配置）：`CONTEXT_LIMIT = 1_000_000`、`COMPRESS_THRESHOLD_PCT = 82`、`ALERT_THRESHOLD_PCT = 75`

**配置生效机制（已验证）**：保存插件配置后，AstrBot 会**自动热重载该插件**（`save_plugin_configs_from_dashboard_payload` 源码确认），无需重启整个程序。

---

## 六、技术方案

### 架构

```
astrbot_plugin_token_monitor/
├── main.py                  # 插件入口：Star 类、告警定时任务、生命周期
├── metadata.yaml            # 插件元数据
├── _conf_schema.json        # 配置 schema（UI 自动生成表单）
├── pages/
│   └── token-monitor/       # 插件页面（AstrBot 自动发现）
│       ├── index.html
│       ├── app.js           # 页面逻辑：apiGet 拉数据 + 渲染 + 10s 轮询
│       └── style.css        # 样式（先简洁，后美化）
└── README.md
```

### 关键技术点（均已验证可行）

1. **页面调用接口**：桥接 SDK `window.AstrBotPluginPage.apiGet(endpoint)` 代理请求，**免 CORS 免鉴权**，直接调 `/api/conversations` 和 `/api/stats/provider-tokens`
2. **主动发消息**：`Context.send_message(session_str, MessageChain)`（已验证 API 存在）
3. **定时检查**：`asyncio.get_running_loop().call_later()` 或 `asyncio.create_task` 循环（proactive_chat 插件同款写法）
4. **配置读取**：`AstrBotConfig` 注入（`__init__(self, context, config)`），配置保存后插件自动热重载生效
5. **数据获取（后端告警逻辑）**：直接查 `data_v4.db`（sqlite3 只读）或复用内部 service——开发时二选一，倾向直接读库（简单、不依赖 HTTP 自调用）

### 数据流

```
页面（iframe）──apiGet──▶ dashboard API ──▶ conversations / provider_stats 表
后端定时任务 ──读库──▶ token_usage ──超 75%?──▶ send_message ──▶ QQ 提醒
```

---

## 七、验收标准

1. **页面可达**：插件安装后，dashboard 插件管理 → 本插件 → 页面可正常打开
2. **数值准确**：页面显示的会话 token_usage 与 `GET /api/conversations` 返回一致（刷新后同步）；趋势图与 provider-tokens 接口一致
3. **列表完整**：所有会话都列出，低用量会话默认折叠，可展开；超阈值会话显示告警徽标
4. **告警生效**：验收时临时将代码中 `ALERT_THRESHOLD_PCT` 调低（如 5%，验收后改回 75%）→ 1 分钟内收到 QQ 告警消息；水位低于阈值后不再收到
5. **去重正确**：持续超阈值期间不重复告警；水位回落（如压缩）后再次超阈值能重新告警；多会话同时超阈值各自告警一次
6. **配置可改**：配置 UI 中开关告警、改检查间隔，保存后插件自动热重载生效（无需重启程序）；上限/阈值等固定值不出现在配置项中
7. **无副作用**：AstrBot 核心代码零修改；插件卸载后无残留
8. **健壮性**：后端接口不可用时页面显示友好提示并自动重试，不白屏、不报错刷屏

---

## 八、红线与限制

- **不修改** `core/astrbot/` 下任何文件
- 插件只**读**数据库，不写任何业务数据
- 告警消息频率受限（按会话独立去重），不会刷屏
- 若 AstrBot 版本升级改变了接口/表结构，插件需适配（开发时尽量用稳定 API）
- 页面静态资源由 AstrBot 托管，**无需重新编译 dashboard**

---

## 九、开发计划（codex cli）

按"需求 → 实现 → 验收"三步：

1. **阶段 1（骨架）**：codex 生成插件目录骨架（main.py + metadata.yaml + _conf_schema.json + 空页面），安装到 `core/data/plugins/`，验证页面入口出现
2. **阶段 2（页面）**：实现页面数据拉取 + 列表/进度条/趋势图/告警徽标 + 轮询 + 手动刷新；林在 dashboard 验收数值准确性
3. **阶段 3（告警）**：实现后端定时检查 + QQ 推送 + 按会话去重；林按验收标准 4/5 测试
4. **阶段 4（打磨）**：折叠交互、视觉风格（另行讨论）、README

每阶段结束由林验收通过后进入下一阶段。

---

## 十、待确认（已收敛）

1. `alert_target_session` 的默认值：林的 QQ 私聊 session 字符串（平台前缀取决于当前使用的 QQ 接入方式，如 `aiocqhttp` / `qq_official`）——开发时确认
2. 趋势图用简单 canvas 自绘还是引入轻量图表库（如 Chart.js，需离线可用）？——倾向自绘或极简方案，不引入大依赖
3. 插件页面命名、显示名（如"Token 水位监控"）——林可指定

> ✅ 已定：告警阈值 = 上限的 75%（比压缩阈值 82% 提前 7%），固定常量，不设配置项。

---

## 十一、已识别但未承诺（可选增强，本次不做）

以下为通读文档时识别出的潜在增强，**本次均不做**，记录备查，功能跑通后按优先级讨论：

1. **最近告警记录**：插件把每次告警写入日志/文件，页面展示"最近告警历史"——可追溯"上次快压是什么时候"
2. **压缩事件标注**：从 `token_usage` 的骤降突变推断"该会话刚被压缩过"，页面标注（如"⚠️ 3 小时前压缩过"）——注意需防误报
3. **会话分组**：列表按私聊/群聊/其他分组展示，配合会话数量增多后的可浏览性
4. **多告警目标**：当前仅单个目标会话；未来可支持同时发到多个会话/群
5. **水位增速与预计剩余时间**：根据历史增量估算"按当前速度还有多久到阈值"——实现需额外记录水位快照，成本较高
6. **会话搜索/过滤**：会话很多时按名称/平台筛选
